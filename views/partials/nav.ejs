<nav class="navbar">
    <div class="navbar-container">
        <div class="navbar-brand">
            <h2>Judge Portal</h2>
        </div>

        <button class="navbar-toggle" id="navbar-toggle" aria-label="Toggle navigation">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <div class="navbar-menu" id="navbar-menu">
            <a href="/dashboard" class="navbar-item <%= currentPage === 'dashboard' ? 'active' : '' %>">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none"
                    stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                    <polyline points="9 22 9 12 15 12 15 22"></polyline>
                </svg>
                <span>Dashboard</span>
            </a>

            <a href="/skipped" class="navbar-item <%= currentPage === 'skipped' ? 'active' : '' %>">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none"
                    stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <polygon points="19 20 9 12 19 4 19 20"></polygon>
                    <line x1="5" y1="19" x2="5" y2="5"></line>
                </svg>
                <span>Skipped</span>
            </a>

            <a href="/assignment-sheet" class="navbar-item <%= currentPage === 'assignment-sheet' ? 'active' : '' %>">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none"
                    stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
                    <polyline points="14 2 14 8 20 8"></polyline>
                    <line x1="16" y1="13" x2="8" y2="13"></line>
                    <line x1="16" y1="17" x2="8" y2="17"></line>
                    <polyline points="10 9 9 9 8 9"></polyline>
                </svg>
                <span>Assignment Sheet</span>
            </a>
            <a href="/tutorial" class="navbar-item <%= currentPage === 'tutorial' ? 'active' : '' %>">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none"
                    stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
                    <polyline points="14 2 14 8 20 8"></polyline>
                    <line x1="16" y1="13" x2="8" y2="13"></line>
                    <line x1="16" y1="17" x2="8" y2="17"></line>
                    <polyline points="10 9 9 9 8 9"></polyline>
                </svg>
                <span>Tutorial</span>
            </a>
        </div>
    </div>
</nav>

<div class="navbar-overlay" id="navbar-overlay"></div>

<script>
    const navbarToggle = document.getElementById('navbar-toggle');
    const navbarMenu = document.getElementById('navbar-menu');
    const navbarOverlay = document.getElementById('navbar-overlay');

    function toggleNavbar() {
        navbarMenu.classList.toggle('active');
        navbarOverlay.classList.toggle('active');
        navbarToggle.classList.toggle('active');
        document.body.style.overflow = navbarMenu.classList.contains('active') ? 'hidden' : '';
    }

    navbarToggle.addEventListener('click', toggleNavbar);
    navbarOverlay.addEventListener('click', toggleNavbar);

    // Close navbar when clicking on a link (mobile)
    document.querySelectorAll('.navbar-item').forEach(item => {
        item.addEventListener('click', () => {
            if (window.innerWidth <= 768) {
                toggleNavbar();
            }
        });
    });
</script>

<style>
    :root {
        --violet: #8b5cf6;
        --black: #0a0a0a;
        --white: #ffffff;
        --text: #a0a0a0;
    }

    .navbar {
        background-color: #1a1a1a;
        border-bottom: 2px solid var(--violet);
        position: sticky;
        top: 0;
        z-index: 1000;
        width: 100%;
    }

    .navbar-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 1rem 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .navbar-brand h2 {
        color: var(--violet);
        font-size: 24px;
        font-weight: 600;
        margin: 0;
    }

    .navbar-toggle {
        display: none;
        flex-direction: column;
        background: none;
        border: none;
        cursor: pointer;
        padding: 0;
        z-index: 1001;
    }

    .navbar-toggle span {
        width: 25px;
        height: 3px;
        background-color: var(--violet);
        margin: 3px 0;
        transition: all 0.3s ease;
        border-radius: 3px;
    }

    .navbar-toggle.active span:nth-child(1) {
        transform: rotate(45deg) translate(8px, 8px);
    }

    .navbar-toggle.active span:nth-child(2) {
        opacity: 0;
    }

    .navbar-toggle.active span:nth-child(3) {
        transform: rotate(-45deg) translate(7px, -7px);
    }

    .navbar-menu {
        display: flex;
        gap: 2rem;
        align-items: center;
    }

    .navbar-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--white);
        text-decoration: none;
        padding: 0.75rem 1.25rem;
        border-radius: 8px;
        transition: all 0.3s ease;
        font-size: 16px;
        font-weight: 500;
    }

    .navbar-item:hover {
        background-color: rgba(139, 92, 246, 0.1);
        color: var(--violet);
    }

    .navbar-item.active {
        background-color: var(--violet);
        color: var(--white);
    }

    .navbar-item svg {
        flex-shrink: 0;
    }

    .navbar-overlay {
        display: none;
    }

    /* Mobile Styles */
    @media screen and (max-width: 768px) {
        .navbar-container {
            padding: 1rem;
        }

        .navbar-toggle {
            display: flex;
        }

        .navbar-menu {
            position: fixed;
            top: 0;
            right: -100%;
            width: 280px;
            height: 100vh;
            background-color: #1a1a1a;
            flex-direction: column;
            padding: 5rem 1.5rem 2rem;
            gap: 0.5rem;
            transition: right 0.3s ease;
            border-left: 2px solid var(--violet);
            align-items: stretch;
        }

        .navbar-menu.active {
            right: 0;
        }

        .navbar-item {
            width: 100%;
            justify-content: flex-start;
            padding: 1rem;
        }

        .navbar-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background-color: rgba(0, 0, 0, 0.7);
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
            z-index: 999;
        }

        .navbar-overlay.active {
            opacity: 1;
            visibility: visible;
        }
    }
</style>